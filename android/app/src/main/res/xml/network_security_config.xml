<?xml version="1.0" encoding="utf-8"?>
<!--
  The app talks to the on-device llama.cpp server over plain HTTP on 127.0.0.1.
  Release builds disable cleartext traffic by default, which would block that loopback
  call. Permit cleartext ONLY for localhost; everything else stays HTTPS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
